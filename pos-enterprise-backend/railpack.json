{
    "$schema": "https://schema.railpack.com",
    "provider": "php",
    "build": {
        "packages": ["composer"]
    },
    "deploy": {
        "startCommand": "php artisan serve --host=0.0.0.0 --port=$PORT"
    }
}
